# 地-墙过渡 · 实验台账（步骤与记录）

> 这份文档只管**做什么、怎么做、量到了什么**。设计、几何推导、原因分析在
> [`WALL-MOUNT-OPEN.md`](WALL-MOUNT-OPEN.md)（那份是这条线的真源），这里只留结论指针。
> 分支 `wall-mount-transition`，未合并 main。每次实验**跑之前先 `git pull`**。

---

## 0. 通用约定

**供电**：走充电宝那条路。启动死机线未结案，口径是"补足充电宝真实脚本 5 次之前，
别在 XL4015 上跑真实脚本"。脚本启动会打印 Pi 欠压粘滞位，有 ⚠ 先别跑。

**安全**：安全绳绷紧，人在旁边。有足在墙上时 `ESC`×2 会放气 = 坠落，先扶稳；
取机用 `o`×2。

**通则**：钢尺立在地面那块玻璃上。**手不要扶机身**——一扶受力就变了。

⚠ **E1~E5b 用的旧量法是"机身前缘中点下沿"，09-12 晚起作废**（旧数据仍按旧口径读，
见 §1 台账里的高度表）。它的毛病：那个点比模型的髋平面高多少从没量过，
反算机身下沉/吸盘真实倾角时这个常数取 38 还是 48，结果差一倍（§4 结论 6）。

**机身位姿量法（09-12 晚改定：量六个 F 点）**

**F = femur 舵机输出轴的盘心**（`LEG-GEOMETRY-OPEN.md` §2.12 高差法里那个 F 点），
每条腿一个，六个。量它离**地面那块玻璃**多高。

为什么是 F：运动学里 `leg_ik` 的 `d = hypot(r, z)` 注释写的就是"距 **femur** 轴"，
竖直链条 `z = l2·sinα + l3·sin(a_t)` 是从 femur 轴往下数的 ⇒ **模型的 z=0 平面，
物理上就是六个 F 所在的平面**，`stand_height=90` 是"F 到足端"的竖直距离。
所以量 F 直接读出机身高，**不再需要"测点比髋平面高多少"那个从没量过的常数**
（就是它让 §4 结论 6 的真实倾角取 38 还是 48 差一倍）。

⚠ coxa 轴是**竖直**的，转 coxa 不改变 F 的高度 ⇒ **F 在机身系里恒在 z=0 平面上，
与 γ 无关**；随 γ 变的是 F 的**水平**位置（半径 43mm = `coxa_len` 的圆）。
换算成世界高度时 γ 通过 x_F 进来，但 γ 是命令量、查下表即可，不用量。

> **h_F(腿) = zb + x_F(腿)·sin(俯仰) − y_F(腿)·sin(横滚)**

| 状态 | | L1 | L2 | L3 | R1 | R2 | R3 | 前后 x 跨距 |
|---|---|---|---|---|---|---|---|---|
| **H0 / 就位暂停**（γ 全 0） | x_F | +108 | **0** | −108 | +108 | **0** | −108 | 216 mm |
| | y_F | +98 | +125 | +98 | −98 | −125 | −98 | |
| **H5 终态**（前 γ−55 / 后 +55 / 中 ∓28.8） | x_F | +127 | +21 | −127 | +127 | +21 | −127 | 253 mm |
| | y_F | +63 | +119 | +63 | −63 | −119 | −63 | |

三个读数，一把钢尺：

- **俯仰** = asin[(后 h_F − 前 h_F) ÷ 跨距]。H0 跨距 216（1° = 3.8 mm）；
  H5 终态跨距 253（1° = 4.4 mm）。**低头（前低）为负，抬头为正。**
- **机身高 zb** = 前后四个 F 高度的平均（x_F 前后对称，俯仰自动抵消）。
  H0 更省事：**中腿 x_F = 0，h_F(L2) 与 h_F(R2) 取平均就是 zb**。
- **横滚** = asin[(左 − 右) ÷ (2·|y_F|)]。

**H0 该读到多少**：模型把足端命令到 F 平面下 **108 mm**（站高 90 + 压入 18），足端顶在
玻璃上下不去 ⇒ h_F 会落在 **93~108**。读出来是多少，就直接告诉我们那 18 mm 压入
预载被腿链让掉了多少——这个数从 08-19 起一直是猜的。

要量的状态：**就位暂停 / H0 六足 / H2 L1 上墙 / H4 双前足上墙 / H5 后足指正后 /
逐级抬头每档**。手机水平仪贴机身顶面可以互相印证（角度直读，但读不出 zb）。

**中腿压不压得住（纸条法）**：一条纸条塞到中腿吸盘下面，能轻松抽出来 = 它没压住。
每个俯仰档位查一次。

**状态行速查**：每条腿两个字符 = 相位 + 吸附态。
`·`=站立 `Z`=零力交接 `z`=接管 `V`=放气 `L`=抬起 `T`=平移 `H`=悬停 `D`=下探
`P`=压入 `W`=等吸附 `^`=收在空中；`A`=已吸附 `r`=未吸 `v`=放气中 `s`=抽气中
`!`=漏气。`盘差` = **所有已吸附的盘里最弱的那一只**（"差"是最差的差），
正常 −60~−75，浅于 −50 过不了抬腿门槛，浅于 −20 触发漏气监护。
中腿是 `--support-legs`，常态就是 `·r`（不吸），**不会**出现在 `盘差` 里。

**电流**：`泵开` 时比 `泵停` 时高零点几安，记电流必须同时记泵的状态，否则不可比。

**抬头步长**：`↑` 是整档铺设，**够不到整档就整档被拒**——所以你总是停在天花板下面
不到一整步的地方（E5：天花板 6~7°，5° 一档停在 5°；落点 235 时天花板 14°，5° 一档
只能停在 10°）。要贴着天花板量就用 `--pitch-step 2`。

**日志**：`software/logs/mount_*.log`，每拍有逐腿相位/盘压/交接偏移/电流。
跑完 scp 到开发机（`64.176.60.202`）——**A 组那次没留，别再丢了**。

**通用中止条件**（任一出现就停）：
- 被抬的那条腿在 `Z` 段变 `!`，或 `盘差` 快速往 −30 爬 → δ 把盘拔起来了
- 屏幕出现 ⚠ 开头的行（交接截断/接管塞不下）→ 抄下来
- 冻结 → 先看清原因再按 `f`（注意 `f` 会**续铺**在途的交接，不会取消）
- 接管后电流不回落 → 在堵转顶玻璃，退回去
- 机身肉眼可见往墙上贴或歪 → 空格 / `o`×2

---

## 1. 已做实验台账

| 编号 | 日期 | 做了什么 | 结果一句话 | 详情 |
|---|---|---|---|---|
| E1 | 09-09 | 单前足上墙（首次） | 能吸住；查出 L1 软管折死、前伸够不到 17 mm、机身下沉 32 mm 不回弹 | OPEN §4 |
| E2 | 09-11 | **A 组**：中腿前移 + 双前足上墙，不开交接 | 抬第二只前足沉 **15 mm** 且不回弹；`]` 实现率 ≈100% 但只有 15 mm 可用 | OPEN §4.1 |
| E3 | 09-11 | **B 组**：同参数 + 零力交接/接管 | 同一步沉 **5 mm**（−67%）；电流 1.24→0.65 A；δ=10 无拔盘 | OPEN §4.2 |
| E4 | 09-12 | 台架两项 | a：盘斜 35° 仍压得住（→ `SUPPORT_TILT_DEG=35`）；b：16mm 是前腿标定，重标后 trim 归零 | §2 |
| E5 | 09-12 | 调参重跑 + 逐级抬头 | 密封/标定两项都过；**抬头只到 5°**（预期 15°）——**落点取了带中点不是带上沿**，那 8° 是自己丢的 | §3 |
| E5b | 09-12 | 落点 235 + 步长 2° 重跑 | **三条判据全过**：命令抬到 **14°**、中腿全程压得住、下沉回到 5 mm | §4 |
| E5c | 09-12 | 补量机身倾角（同一次实机，事后加量） | **真实俯仰 = 0.891×指令 − 3.0°**；指令 14° ⇒ 真实 **9.48°**。那 −3° 静态低头是最便宜的一笔 | §4 结论 4 |

**E2/E3 高度记录**（机身前缘中点离地 mm）：

| | H0 六足吸住 | H1 中腿前移 | H2 L1 上墙 | z×2 | H3 R1 悬停 | H4 R1 吸住 |
|---|---|---|---|---|---|---|
| A 组 | 143 | 143 | 134 | — | 119 | 119 |
| B 组 | 139 | 141 | 136 | 135 | 130 | 128 |

**E3 用的参数**（基线，后续对照用）：
```
--no-tank --wall-dist 155 --floor-clear 45 --support-legs L2,R2 \
--wall-trim L1:16,R1:0 --handover L1:10,R1:10,L3:8,R3:8,L2:5,R2:5 \
--takeover L1:10,R1:10
```
实验中现场调出来的：R1 落点要抬 **10 mm** 才与 L1 齐平。
⚠ 我此前从日志推的"R1 墙面修正 −8"是**误读**——那 4 次 `,` 是按"盘贴玻璃时先退开再调
落点高度"的操作纪律做的，R1 本来就刚好停在命令的 15 mm 上。

---

## 2. E4：台架两项（09-12 已做）✅

### E4a 吸盘斜着压还能不能压住 —— **过了**

| 倾角 | 35° |
|---|---|
| 接触部位 | **盘面** |
| 还压得住吗 | **压得住** |

θ_press ≥ 35°，而 35° 正是中腿在俯仰 90° 时盘面最多斜到的角度（coxa ±55° 口径，
OPEN §1.1）⇒ **倾角这条永远不卡中腿**。

**已据此改引擎**：只承重腿单独一个倾角容差 `SUPPORT_TILT_DEG = 35`（`--support-tilt`
可调），不再套吸盘密封那条 15°。仿真复核：放宽之前中腿正好在 15° 那一档卡住
（`L2 吸盘倾角 15°（面外 15°）`），放宽之后瓶颈回到前腿。

### E4b 查 L1 那 16 mm —— **结案：前腿标定**

用户重标了两条前腿的 `attach_deg`（提交 `b68da24`）：L1 coxa −3° / femur +3° /
tibia −4°，R1 coxa −3° / femur −1.5°。**现在两边都对准了**，`--wall-trim` 双双归零。

收益：trim 16 自己就吃掉 5° 抬头余量，归零等于**白捡回 5°**（仿真：dist 140 下天花板
10° → 15°）。

⚠ **我之前那条"疑似机器人偏航 11°"的线索是错的**，它建立在"R1 需要 −8"这个从日志
误读来的数上（见 §1 的更正）。真相就是 OPEN §7 一开始立的那条判据说的：只有一条腿偏
⇒ 该腿标定。

---

## 3. E5：调参重跑 + 逐级抬头（09-12 已做）——**抬头只到 5°，原因是落点**

一次跑完回答四件事：①两个余量问题解没解开 ②抬头能到几度 ③**中腿在那个俯仰下还压不
压得住地面**（静力那条悬案的第一手数据）④（可选）收中腿前后的对照。

⚠ **09-12 起：中腿不收了。** 原来收它的唯一理由是"φ∈(30°,70°) 盘面对不正、吸不上"，
而中腿本来就不吸（只压），E4a 已证明只压不吸到 35° 倾角都成立——那个理由塌了。
仿真复核：**全程不收中腿能抬到 35°**（收的话 15° 就得转四接触），而且 35° 时卡的是
**后腿**（吸附腿，15° 容差）不是中腿。保持压着的收益是载荷摊在 6 条腿上，
把那个 20~23 N 单墙盘剪切稀释掉；代价接近零——中腿不参与互锁、不参与漏气监护，
即便高俯仰下它变成不承力，也只是跟着走。
中腿真正必须收的时机是**髋高超过 ~195 mm**（扶梯末段身体升到 250 时），不是某个俯仰角。

### 参数

```bash
python scripts/mount_wall.py --no-tank --wall-dist 140 --press-delta 13 \
    --floor-clear 45 --support-legs L2,R2 \
    --handover L1:10,R1:10,L3:8,R3:8 --takeover L1:10,R1:10
```

⚠ **这条命令少了一个参数，就是它把抬头砍掉 8°**：没给 `--wall-height`，脚本按缺省
取可落足带**中点**（实跑=218），而下面那张站位窗口表和所有仿真预期都是按
**落带上沿**（≈235）算的。补 `--wall-height`，理由见 §3 结论。

与 E3 的差别，逐条都有理由：

| 改动 | 为什么 |
|---|---|
| `--wall-dist` 155 → **140** | 站近换抬头余量。⚠ **这个数是实测值不是设定值**——放哪儿就填哪个数（就位暂停按 `d` 改），但**站位要落在 120~140 这个窗口**里，见下表 |
| `--press-delta` 18 → **13** | 地面腿压深余量 10 → 15 mm；不改的话收中腿时 `L3 压入 28 超上限` 当场被拒 |
| 中腿 δ 5 → **0**（不写进 `--handover` 即为 0） | 中腿没真空、没有密封破裂，交接对它们没有"消灭放气弹跳"的作用；而收起后这笔账**还不回来**，纯亏 |
| **不给 `--wall-trim`**（E3 给的是 L1:16） | E4b 把前腿标定修好了，两边都对准；trim 会叠进压深还吃 5° 抬头余量，能不用就别用 |
| （新）`--support-tilt` 默认 35 | E4a 的结果已进默认值，不用显式给；中腿不再被倾角卡 |

⚠ **`--press-delta 13` 是这次唯一有风险的改动**：压入行程小了，密封可能变差。
盯启动六足吸附是不是还一次过、`盘差` 能不能压到 −70 以下。吸不上就退回 15 或 18
（退回去的话，收中腿那步会被拒，那就不收中腿、只做到第 3 步为止）。

⚠ **重点验标定**：悬停时目测两只前盘离墙是不是都 15 mm。要是还差，用 `.`/`,` 现场调
并**记下来**——那说明标定还没到位，抬头余量会跟着少。

**站位窗口**（新标定实测扫描，press 13 / 前腿 δ=10 / **前足落带上沿** / 中腿不收）：

> ⚠ 09-12 补：**"落带上沿"这四个字是这张表的前提，不是可选项。** 脚本缺省取带
> **中点**，两者差 8° 抬头——E5 就栽在这儿。这张表下面每一个"俯仰上限"都要配
> `--wall-height <带上沿>` 才成立。

| 距墙 | 110 | 120 | 130 | **140** | 150 | 155 | 160 | 170 | 180 |
|---|---|---|---|---|---|---|---|---|---|
| 前腿可落足带 | — | 200~240 | 200~240 | 200~240 | 200~235 | 200~230 | 200~230 | 200~215 | 200~205 |
| 六接触俯仰上限 | **上墙被拒** | 15° | 15° | **15°** | 10° | 10° | 10° | 5° | 0° |

110 处上墙被拒的原因是"摆动路径第 0 点足端撞墙面"——腿一抬起来就蹭玻璃，所以**窗口
两头都有界**。120~140 是一个平台（带和天花板都一样），选 **140** 是因为它在平台的
**远端**：离"太近撞墙"那条线有 20~30 mm 余量，量不准或摆歪一点也还在窗口内，手也好伸。
⇒ **目测放到 13~14 cm，量出来多少填多少，落在 120~140 就不用挪**；量到 155
（E3 那次的站位）就该往前挪，那里天花板只有 10°，进不了扶梯入口。

**仿真给的预期**（新标定 + trim 0 + press 13 + 中腿 δ=0 + **落带上沿**）：抬头到
**15°** 被 `L1 吸盘倾角超 15°` 拒；中腿全程不是瓶颈（放宽之前它正好卡在 15°）。
15° 正是扶梯入口，再往上要靠把前足往墙上挪（E6）。
（实跑取了带中点 ⇒ 只有 6~7°，预期没错，是命令行少了一个参数。见下面的结论。）

### 步骤

1. 就位暂停：量前髋到玻璃的距离，按 `d` 输实测值，`p` 启动
2. 六足吸住 → 量 **H0**；`盘差` 记一次（验证 press 13 的密封）
3. `2` `t` → 等悬停 → `i` → 等收口；`5` `t` `i` → 量 **H1**
4. `1` `w` → **目测离墙间距（验标定：应当就是 15 mm，不用调）**，不对才用 `.`/`,`
   并记下 → `i` → 量 **H2**
5. `4` `w` → 悬停 → 目测与 L1 齐平，不齐用 `+`/`-` → 量 **H3** → `i` → 量 **H4**
6. `3` `b` `i`、`6` `b` `i`（后足指正后）→ 量一次高度
7. **逐级抬头**：`↑` 一次（5°）→ 等"位姿铺完" → 每一档记一行（见下表）→ 再 `↑`。
   直到被拒（预期在 10~15° 被 `L1 吸盘倾角超 15°` 拒）
8. **（可选）收中腿的对照**：`2` `h`、`5` `h` → 量高度、记电流 → 再试 `↑`。
   ⚠ **现在没有理由收中腿**（见下），这一步纯粹是取个对照数（收了之后载荷全压到
   4 条腿上，电流/高度跳多少）。不想冒这个险就跳过。
9. `o`×2 取机 / `ESC`×2 退出；日志传过来

### 记录

实跑命令（`software/logs/mount_20260912_121413.log`，git `0b195b4`）：

```
--no-tank --wall-dist 145 --press-delta 13 --floor-clear 45 --support-legs L2,R2 \
--handover L1:10,R1:10,L3:8,R3:8 --takeover L1:10,R1:10
```
（站位实测 145 —— 在 120~140 窗口外沿一点点，但下面会看到，这 5 mm 不是瓶颈。
**没给 `--wall-height`**，脚本取带中点：L1 落 218，R1 落 218 后现场 `=` 抬 5 到 222。）

**上墙段**（同 E2/E3 口径，直接和上表对照）：

| | H0 六足 | H1 中腿前移 | H2 L1 上墙 | H3 R1 悬停 | H4 R1 吸住 | 6 步后 |
|---|---|---|---|---|---|---|
| A 组（E2） | 143 | 143 | 134 | 119 | 119 | — |
| B 组（E3） | 139 | 141 | 136 | 130 | 128 | — |
| **E5** | **138** | **137** | **129** | **117** | **117** | **111** |

最终 trim：L1 **0** R1 **0**（全程没按过 `.`/`,`）　　启动首吸盘压：**−62.5 ~ −73.9 kPa**，
六足一次过，滞环稳定在 −60/−75。

**逐级抬头段**（第 7 步）：

| 俯仰 | 高度 mm | 盘差 | 电流（泵停） | 屏幕 |
|---|---|---|---|---|
| 0° | 111 | −60~−76（滞环） | **1.34 A** | — |
| 5° | 未量 | −60~−76（滞环，没变差） | **2.23 A** | — |
| 10°（请求） | — | — | — | **拒：位姿不可行（俯仰 7.0°）：L1 吸盘倾角 15°（面外 0°）超 15°** |

中腿纸条：未做。收中腿对照（第 8 步）：未做。

### 判据对表

- **①余量 —— 过。** press 13 下六足一次吸上、盘压正常，密封没变差；trim 双双 0，
  E4b 的标定结论成立（悬停时不用调落点）。
- **②抬头 —— 只到 5°，没到 15°。** 但**不是标定或站位的问题**（判据里写的两个猜测都
  不对），见下面的结论。
- **③中腿 —— 没测到。** 抬头只有一档，纸条没做。这条仍然空着，是 E5b 最该补的。
- **④收中腿前后 —— 跳过**（现在没有理由收）。

### 结论：那 8° 是自己丢的，落点高度就是抬头预算

拿日志的状态在引擎里离线复算，**一模一样复现**（俯仰 7.0°、L1 倾角 15.1°、面外 0°）。
逐项拆开看，卡住的原因全在**前足落在墙上的高度**：

| 口径 | L1 落点 | 接管 | 抬头上限 |
|---|---|---|---|
| 实跑 | 218（带中点，缺省） | 10 mm | **6~7°** ← 实机在 7.0° 被拒 |
| 同一次，若接管给 0 | 218 | 0 | 11° |
| 计划里那套（站位窗口表的口径） | 235（**带上沿**） | 10 mm | **14°** |
| 站位挪回 140 + 带上沿 | 240 | 10 mm | 17° |

规律只有一条，**落点高度、接管量、抬头，三者花的是吸盘倾角那同一笔 15° 预算**：

> **落点每高 1 mm ≈ 多 0.45° 抬头；接管每 1 mm ≈ 少 0.45° 抬头。**
> "吸盘轴⊥墙"的落点在 **≈224**（跟墙距几乎无关）。抬头会把盘面往同一个方向扳，
> 所以落点要**故意放得比 ⊥ 点高**，把倾角先扣在反方向上，抬头才有得花。

带中点（218）比 ⊥ 点还低 6 mm，再加接管 10 mm，等于开局就欠了 16 mm ≈ 7°，
15° 的预算只剩 7° 给抬头。**站位 145 vs 140 只值 1°，press 13 与标定都无辜**。

⇒ 代码里补了两件事（这次实机的直接产出）：
`MountEngine.wall_perp_height()`（⊥ 点）与 `wall_pitch_room()`（这条腿的倾角还允许抬
多少度），按 `w` 选落点时一起打印出来，下次不会再默认着默认着就把 8° 丢了。

### 另外三条读数（都写进 OPEN）

1. **抬头 5° 把整机电流从 1.34 A 推到 2.23 A（+66%，泵停口径）。** 这是**六接触**
   （中腿没收、还压着）的读数，载荷账第一次有了实测数：俯仰一上去，舵机吃的力
   明显变大。
2. **下沉比 E3 差**：H2→H3（抬第二只前足）沉 **12 mm**，E3 同一步只沉 5 mm；
   H0→H4 累计 21 mm vs E3 的 11 mm。交接/接管参数一字没改，变的是
   `press_delta 18→13`、`wall_dist 155→145`、`wall_trim L1 16→0`。
   **最可疑的是 press 13**：地面腿的压入预载少了 5 mm，支撑那一摞更软。E5b 顺带对照。
3. **模型算的倾角是"命令"口径，不是吸盘真实的角度。** 实机目视 L1 盘始终垂直于墙面
   ——这个观察是对的，而且能对上数：按实测的下沉 21 mm 反推，俯仰 7° 那一刻 L1 盘的
   **真实**轴几乎正对着墙，模型报的 15° 是"命令点比吸盘实际位置低 31 mm"折算出来的角。
   详见 OPEN §4.3。
   ⚠ **09-12 补正**：这个反推依赖一个从没量过的常数（前缘测点比髋平面高多少，
   我取的 48），换成 38 结果就差一倍——**方向结实、数值不结实，当时写的"恰好 0.0°"
   是过度精确**。要变成准数只需量一次髋高，见 §0。

---

## 4. E5b：把落点放对再抬一次（09-12 已做）✅ **三条判据全过，抬到 14°**

E5 只差一个参数。这次把 E5 没答上的三件事一次补齐：**抬头到底能到几度、中腿在那个
俯仰下还压不压得住、下沉是不是被 press 13 弄坏的**。

**结果：三条全过**（日志 `mount_20260912_145440.log`，git `19077d3`）。抬到 **14°**、
中腿全程压得住、抬第二只前足的下沉回到 **5 mm**。干跑预测的 14° 与实机一字不差。
**新挖出一条**：高度阶梯说明机身只实现了**约一半**的指令俯仰（见结论第 4 条）。

### 参数

```bash
python scripts/mount_wall.py --no-tank --wall-dist 140 --press-delta 18 \
    --wall-height 235 --pitch-step 2 --floor-clear 45 --support-legs L2,R2 \
    --handover L1:10,R1:10,L3:8,R3:8 --takeover L1:10,R1:10
```

**这条命令离线干跑过**（真引擎 + MockVacuumIO 走完整条按键序列）：六足启动 → 中腿前移
→ 双前足上墙 235 → 后足指正后 → 逐级抬头，**抬到 14° 被 `L1 吸盘倾角超 15°` 拒**，
中途无截断、无冻结。

与 E5 的差别，逐条都有理由：

| 改动 | 为什么 |
|---|---|
| **新增 `--wall-height 235`** | E5 的全部损失都在这。抬头上限 6~7° → **14°**。选 235 而不是带上沿：站位落在 140 或 145 时带上沿分别是 240/235，**235 两边都在带内**，不用现场改命令行。⚠ 代价：落点倾角从 1.9° 涨到 6.3°（容差 12°），密封余量变小——悬停时目视盘面对不对正，压下去吸不上就退到 230（抬头 12°，落点倾角 3.4°）。站位确实量到 140 且 235 吸得顺，下一次可以再试 240（17°） |
| `--press-delta` 13 → **18** | E5 的下沉（H0→H4 −21 mm）比 E3（−11）差一倍，press 是唯一动过的"硬度"参数。**先把它退回去做对照**；退回后如果下沉回到 ~11 mm，就锁定 press 是原因。（查过了：press 18 对抬头上限没有影响，落点 235 照样 14°——这两件事不互相干扰，可以同时改） |
| 站位 145 → **140** | 只值 1° 抬头，但 140 在窗口平台的远端，顺手 |
| **新增 `--pitch-step 2`**（缺省 5） | 干跑查出来的：落点放对之后天花板是 14°，但 5° 一档只能停在 **10°**，白丢 4°（E5 那次同理：天花板 6~7°，5° 一档停在 5°）。**最后一档总是差在步长上**。落点 235 下：步长 5→10°、3→12°、2→**14°** |

⚠ press 退回 18 的已知代价：地面腿压深余量回到 10 mm。干跑里后足指正后那两条腿的
**命令压入是 26 mm，离 28 的上限只剩 2 mm**——序列本身跑得通，但**别再对地面腿按 `z`**，
一按就顶到上限。真被"压入超上限"拒了就退回 15（仍比 E5 的 13 硬），别退回 13。

### 步骤

同 §3 的 1~7 步，**第 7 步每一档都要做完这三件事**才按下一次 `↑`：

1. 量高度（机身前缘中点下沿）
2. **中腿纸条**（E5 漏掉的那条，本次最有价值）：L2、R2 各抽一次
3. 抄电流（泵停口径）+ `盘差`

预期在 **14°** 被 `L1 吸盘倾角超 15°` 拒（干跑值）。到不了 10° 就别再猜标定了，
**把拒绝那一行原样抄下来**——那行里的"俯仰 x.x°"和腿名直接对得上离线复算。

### 记录

**上墙段**（与 A/B/E5 同表对照）：

| | H0 六足 | H1 中腿前移 | H2 L1 上墙 | H3 R1 悬停 | H4 R1 吸住 | 6 步后 |
|---|---|---|---|---|---|---|
| A 组（E2，不开交接） | 143 | 143 | 134 | 119 | 119 | — |
| B 组（E3，press 18） | 139 | 141 | 136 | 130 | 128 | — |
| E5（press 13，落点 218） | 138 | 137 | 129 | 117 | 117 | 111 |
| **E5b（press 18，落点 235）** | **138** | **138** | **131** | **126** | **126** | **123** |
| E5b 逐步增量 | — | 0 | −7 | **−5** | 0 | −3 |

前足两次都**一次吸上**（L1 −65.4、R1 −63.9 kPa），全程**没按过任何 `.`/`,`/`+`/`-`**
——落点 235 那条密封风险没出现，标定也再次确认到位。

**逐级抬头段**（步长 2°；盘压与电流从黑匣子逐拍统计，泵停口径）：

| 俯仰 | 前缘高度 mm | 中腿纸条 | L1 | L3 | R1 | R3 | 最差盘 | 电流 |
|---|---|---|---|---|---|---|---|---|
| 0° | 123 | 压住 | −73 | −69 | −70 | −69 | — | **0.97 A** |
| 2° | 125 | 压住 | −75 | −70 | −75 | −71 | −60 | 1.36 |
| 4° | 126 | 压住 | −75 | −68 | −75 | −68 | −61 | 1.60 |
| 6° | 127 | 压住 | −75 | −69 | −75 | −68 | −60 | 1.78 |
| 8° | 130 | 压住 | −75 | −69 | −75 | −68 | −60 | 1.96 |
| 10° | 133 | 压住 | −76 | −68 | −75 | −68 | −61 | 2.09 |
| 12° | 135 | 压住 | −76 | −68 | −75 | −68 | −60 | 2.38 |
| **14°** | **136** | **压住** | −76 | −69 | −75 | −68 | −60 | **2.42 A** |
| 16°（请求） | — | — | — | — | — | — | — | 拒：俯仰 15.0° `L1 吸盘倾角 15°（面外 0°）超 15°` |

屏幕全程无 ⚠（只有两次 Pi 5V 掉到 4.78/4.79V 又恢复，不是死机那条线的签名）。

### 判据

- **抬头**：≥12° → 扶梯入口够用，排 E6。7° 上下 → 落点没起作用，回头查 `w` 那一步
  打印的"倾角允许抬到 x°"与实际拒绝角对不对得上（对不上就是模型的问题，不是机器的）。
- **中腿**：到被拒那一档纸条都抽不动 → "只压不吸能一直撑着"成立，四足阶段那笔
  20~23 N 单盘剪切有望推迟（OPEN §1.1）；中途抽得动了 → 记下角度，OPEN §7
  "中腿只能承压不能承拉"证实，扶梯载荷账按四足重算。
- **下沉**：H2→H3 回到 ~5 mm → press 13 是 E5 变差的原因，以后 press 不许再降；
  仍是 12 mm → 不是 press，回头查 `wall_dist`/`trim`（E8 那条 δ 加大也该提前）。

### 结论（对着上面三条判据）

**1. 抬头 —— 过，到 14°**（判据要 ≥12°）。离线干跑给的就是 14°，实机一字不差，
连拒绝行都一样（`俯仰 15.0° … L1 吸盘倾角 15°（面外 0°）超 15°`）。E5 的 5° → 14°，
**换的只是 `--wall-height` 和 `--pitch-step` 两个参数，机器一个螺丝没动**。
⇒ **E6（换墙面前足）解除前置，可以排**。

**2. 中腿 —— 过，而且是本次最有价值的一条。** 0° 到 14° 每一档纸条都抽不动。
⇒ **"中腿只压不吸能一直撑着"成立**（至少到 14°）。四足阶段那笔 20~23 N 单墙盘剪切
可以摊到 6 条腿上，**E7 的风险被推迟**（OPEN §1.1、§7）。
旁证：四条吸附腿的盘压从 0° 到 14° 纹丝不动（L1/R1 −75~−76、L3/R3 −68~−69，
最差盘一直贴在 −60 的滞环上），**后腿到 14° 都没有被剥的迹象**。

**3. 下沉 —— 过，H2→H3 = −5 mm**，与 B 组一模一样（E5 是 −12）。累计 H0→H4 −12 mm
（B 组 −11、E5 −21）。⇒ **press 13 就是 E5 下沉变差的原因，以后 press 不许再降到 18 以下**
（那 5 mm 压入预载是地面腿"硬不硬"的本钱）。

**4.（新）真实俯仰 = 0.891 × 指令 − 3.0°** —— 09-12 当天补量角度直接定死的，
比上面三条都更要紧。

用户把机身倾角量了两次（不是量高度，是直接量角）：

| | 指令俯仰 | **实测倾角** |
|---|---|---|
| 逐级抬头开始前 | 0° | **−3.0°（低头：前缘低、后缘高）** |
| 按满 7 次 `↑` 之后 | 14° | **+9.48°（抬头，且后缘在降）** |

⇒ 俯仰**摆幅**是 12.48°/14° = **89% 实现率**，外加一个 **−3° 的静态低头**：

> **真实俯仰 ≈ 0.891 × 指令俯仰 − 3.0°**

这条线把前缘高度阶梯整条都对上了（把实测角代回去，再假设下沉随指令线性增长，
7 档残差全在 ±2 mm 内）。之前只量前缘时提的"极端 A：真实俯仰只有 7.7°"**被证伪**——
俯仰基本是真的，缺的那一截是**机身又多沉了 ~8 mm**（极端 B 那一侧）。

⚠ 但**那 −3° 的静态低头是新的、也是最便宜的一笔**：它白白吃掉 3° 真实俯仰，
而且不花任何命令预算。指令天花板 14° ⇒ 真实只有 9.5°，离扶梯入口的 15° 差 5.5°；
**把 −3° 治好就直接补回 3°**。

⚠ **还不知道这 −3° 是哪儿来的**：是站姿本来就低头（那是标定/站位的事），还是
上墙之后前半身悬臂沉下去造成的（那是结构的事）。**下次从 H0 开始每个关键状态都量一次角**
（H0 六足 / H2 L1 上墙 / H4 双前足上墙 / H5 后足指正后 / 再逐级抬头），一次就分得开。

**5.（旁证）落点放对之后，同俯仰下的电流低了约四分之一。**
E5 在 5° 时 2.23 A，E5b 在 4~6° 是 1.60~1.78 A（插值到 5° ≈ 1.69 A，**−24%**）。
与 §3 结论第 3 条同源：落点对了，指令点与吸盘实际位置的错位小，舵机不用那么使劲。
⇒ **落点放对不只买到抬头，还省力。**（⚠ 不是受控对照：press 与站位也变了。）

**6. 模型的倾角判据在这一段是"反着走"的。** 把实测位姿代回引擎反算 L1 吸盘轴与墙
法向的夹角：指令 0° 时**大**、指令 14° 时**小**，而引擎按命令口径算的正好相反
（0.7° → 14.3°）。也就是说**抬头其实是在把吸盘往正的方向扳**，模型却以为在把它拧歪。

⚠ **绝对值别当真**：这个反算依赖一个从没量过的常数——前缘测点比髋平面高多少
（我按 H0=138、髋高 90 取了 48）。它取 38 / 48 时结果是 0°档 10.3°/17.6°、
14°档 1.2°/4.6°，取 58 就直接 IK 不可达。**方向结实，数值不结实。**
⇒ 要把它变成准数只需**量一次髋高**（coxa/femur 轴中心离地，六足站好时量一次，
是机器的常数），见 §0。

不过有一条不依赖那个常数：**指令 0° 那一档，盘在被拧着的同时稳稳吸了好几分钟
（−73 kPa 一路不掉）**，而按任何一种取值那时的真实失配都不小。这是"15° 这条线
卡的是模型不是盘"的实机旁证——正是 E4c 台架要标的东西。

---

## 5. 排队中

| 编号 | 内容 | 前置 |
|---|---|---|
| E5d | **查那 −3° 静态低头是哪儿来的**（最便宜的 3° 真实俯仰）：**从 H0 开始每个关键状态量一次倾角**（H0/H2/H4/H5）。H0 就低头 ⇒ 站姿/标定的事；上墙过程中一步步低下去 ⇒ 前半身悬臂，试加大 `--fwd-dist`（干跑到 150 都摆得到，160 才超 coxa ±60°）。量法用六个 F（见 §0），顺带把欠着的机器常数——**F 平面离地多高**——一并读出来。**⚠ 别为它单开一次实机**——它要的读数在 E6 的上墙段顺路就做完了（那一段两次实验一模一样） | 无 |
| E6 | **换墙面前足**（扶梯第一级）——**已排成完整实验，见 §6**（含干跑预演、参数、步骤、记录表、判据）。一句话配方：俯仰留在天花板 14°、前腿 δ 降到 ≤6、L1 从 235 挪到 270（+35 不是 +50） | E5b 已过（14°）；**把 E5d 的量角/量 F 顺路做掉** |
| E7 | **抬一只前足**（只剩一只墙盘）——全线最大风险：静力算的单盘剪切 20~23 N > 实测容量 15 N | E6；安全绳、低俯仰先做 |
| E4c | **台架：已吸附的盘能吃多大"指令失配"**。贴一只盘在玻璃上，沿盘面切向逐毫米加指令偏移（就是接管在做的事），记多少毫米开始滑、多少毫米开始剥。现在 `HOLD_TILT_DEG=15°` 是从**落点密封**容差抄来的，套在"已经吸住、正在被拧"的盘上没有依据——E5 实测那 15° 里吸盘物理上是正的（§3 结论第 3 条）。有了这个数，接管量和抬头预算才有实测依据 | 无（台架，随时可做，不占机器） |
| E8 | δ 加大再跑一遍（δ=10 无拔盘迹象，余量还在），看 5 mm 还能不能再降 | — |
| E9 | "交接开、`z` 不按"的对照，把手动接管的收益单独分出来 | — |

---

## 6. E6：换墙面前足（实机）——**下一个**

扶梯的第一级：把一只**已经在墙上**的前足往上挪，然后继续抬头。
顺路把 E5d 欠的量角/量 F 一起做掉（上墙段两次实验一模一样）。

### 6.1 干跑预演（09-12，实机前必读）

在 E5b 的**终态**（命令俯仰 14°、落点 235、δ_前 10、接管 10）上，用真引擎试
"把 L1 沿墙往上挪"：

| 挪到 | 落点本身 | 整条命令（含交接与路径） |
|---|---|---|
| 250（+15） | 可行 | **拒**：`L1 零力交接不可行：R1 吸盘倾角 15° 超 15°` |
| 260（+25） | 可行 | 同上 |
| 270（+35） | 可行 | 同上 |
| 285（+50） | **拒**：落点倾角 18° 超 12° | — |
| 300（+65） | **拒**：IK 不可达 | — |

三条结论：

1. **能挪的量是 +35 mm，不是原来写的 +50 mm**（14° 时 L1 的可落足带是 230~270）。
2. **卡住的不是俯仰，是交接**：把 L1 卸 δ=10，其余 5 条腿每条要多接 2 mm，而
   **R1（另一只墙面腿）已经贴在 15° 倾角上**，接不动 ⇒ 整条命令被拒。
3. **退回低俯仰只会更糟**：12° 时落点自己就 12° 超容差，越低越大（10°→13°、
   8°→16°、6°→19°）。往上挪本来就要靠俯仰把腿的姿势摆出来。

⇒ **E6 的配方：俯仰留在天花板上，把前腿的 δ 降到 ≤6**（干跑：δ=6/4/2 在 14° 都受理）。
即 `--handover L1:6,R1:6,L3:8,R3:8`。代价是上墙那一步的卸载量从 10 降到 6
（E3 的 −67% 是按 δ=10 测的），⚠ **这是拿"前足上墙那一步的下沉"换"能不能换墙面前足"**。

**谁在吃这笔预算**（同一次干跑，落点 235 / 站位 140）：

| δ_前 | 接管 | 序列末墙面腿的指令偏移 | 命令天花板 | 真实俯仰 |
|---|---|---|---|---|
| 10 | 10 / 5 / 0 | L1,R1 −10 | 14° | +9.5° |
| 5 | ≤5 | L1,R1 −5 | 16° | +11.3° |
| 0 | 0 | 0 | 18° | +13.0° |

注意 **`--takeover` 单独调没用**：交接欠的账落地后照样要还回来，δ=10 时不管接管设
0 还是 10，墙面腿最后都停在 −10。**真正的旋钮是 δ_前**。

**顺手记一条可以改的代码**（还没改）：`_plan_handover` 现在是"δ 塞不下就拒整条命令"，
而 `_fit_takeover` 早就会二分出"塞得下的最大量"。交接同理——**卸一部分远好过整条命令
做不了**，而且小一点的 δ 仍然是完整的零和交接，不是"半截交接"（那条注释说的是
铺到一半放弃，两回事）。改了之后 E6 不用改命令行也能走。**要不要改由用户定。**

---

### 6.2 参数

```bash
python scripts/mount_wall.py --no-tank --wall-dist 140 --press-delta 18 \
    --wall-height 235 --pitch-step 2 --floor-clear 45 --support-legs L2,R2 \
    --handover L1:6,R1:6,L3:8,R3:8 --takeover L1:10,R1:10
```

与 E5b 只差一处：**前腿 δ 10 → 6**，理由见 §6.1（δ=10 时换墙面前足整条命令被拒）。
⚠ 副作用：上墙那一步（H1→H2、H2→H3）的下沉会变大一些，**那正好是 E8 想要的 δ 对照，
顺手就测了**。

### 6.3 步骤

**上墙段与 E5b 完全相同（1~7 步），但每个关键状态多做一件事：量六个 F**
（量法、换算公式、x_F/y_F 表全在 §0）。

| 步骤 | 动作 | 量什么 |
|---|---|---|
| 0 | 站起后的**就位暂停**（还没按 `p`，六足未吸附） | **六个 F**（用 H0 那行 x_F，γ 全 0） |
| 1 | 按 `d` 填实测墙距 → `p` 启动 | — |
| 2 | 六足吸住 = **H0** | **六个 F** + 首吸盘压 |
| 3 | `2 t i`、`5 t i`（中腿前移） = H1 | 六个 F |
| 4 | `1 w` → 悬停目视 → `i` = **H2**（L1 上墙） | **六个 F** |
| 5 | `4 w` → 悬停 → `i` = **H4**（R1 上墙） | **六个 F** |
| 6 | `3 b i`、`6 b i`（后足指正后） = **H5** | **六个 F**（起用 H5 那行 x_F） |
| 7 | 逐级抬头 `↑`×2° 到被拒 | 每档：六个 F + 中腿纸条 + 电流 + `盘差` |
| **8** | **换墙面前足**：选 L1，`w`，**目标 270**（`--wall-height` 是 235，悬停后用 `+` 按 7 次抬到 270）→ 目视 → `i` | 悬停时目视离墙 15mm；落定后**六个 F** |
| 9 | 再 `↑` 看还能抬多少 | 每档同第 7 步 |
| 10 | `o`×2 取机 / `ESC`×2 退出；日志传过来 | — |

⚠ 第 8 步是**全线第一次抬一只已经吸在墙上的脚**。安全绳绷紧，人扶住。
中止条件除了 §0 那几条，额外加一条：**L1 在 `Z` 段（交接）盘压往 −30 爬就立刻空格**——
墙面腿的储能是切向的，δ 标定值完全没有实测依据（climb 外推说要 28~42mm，这里只给 6）。

### 6.4 记录

**上墙段**（每格填六个 F 的高度 mm，以及由它换算出的 zb / 俯仰）：

| 状态 | L1 | L2 | L3 | R1 | R2 | R3 | zb | 俯仰 |
|---|---|---|---|---|---|---|---|---|
| 就位暂停 | | | | | | | | |
| H0 六足吸住 | | | | | | | | |
| H1 中腿前移 | | | | | | | | |
| H2 L1 上墙 | | | | | | | | |
| H4 R1 上墙 | | | | | | | | |
| H5 后足指正后 | | | | | | | | |

**逐级抬头段**（第 7 步、第 9 步各一张）：

| 俯仰(指令) | zb | 俯仰(实测) | L2 纸条 | R2 纸条 | 盘差 | 电流(泵停) | 屏幕 ⚠ |
|---|---|---|---|---|---|---|---|
| 0° | | | | | | | |
| 2° | | | | | | | |
| … | | | | | | | |
| 被拒于 ____°，原因： | | | | | | | |

**换脚那一步（第 8 步）**：

| | 值 |
|---|---|
| L1 从 235 挪到 ____ | |
| 交接（δ=6）期间 L1 盘压最浅到 | ____ kPa |
| 落定后一次吸上？ | |
| 换脚前后 zb 差（= 这一步沉了多少） | ____ mm |
| 换完之后还能抬到几度 | ____°（指令）/ ____°（实测） |

### 6.5 判据

- **换脚成不成**：L1 能抬起、挪到 270、一次吸上、交接期间盘压不往 −30 爬 →
  **扶梯的一级走通了**，δ_墙 有了第一个实测起点，可以排 E7。
  交接期间盘压掉 → **墙面腿的 δ=6 太大**，退到 4 再试；再不行说明"抬墙面腿"
  这条路本身要重新设计（OPEN §3 那笔剪切账提前到位）。
- **那 −3° 静态低头**（E5d 的问题）：就位暂停/H0 就是负的 ⇒ 站姿或标定的事；
  H0 是 0、到 H2/H4/H5 一步步变负 ⇒ 前半身悬臂沉出来的，看是哪一步掉得最多。
- **换脚买到多少抬头**：换完之后指令天花板应当比换之前高（干跑没算这一段，
  实测多少就是多少）。买不到 ⇒ 扶梯"抬到极限→换脚→再抬"这个循环不成立，
  整条路线要重想。

---

## 7. 记录模板（新实验照抄）

```
## Ex：<一句话目的>（日期）
参数：<完整命令行>
与上次的差别：<逐条+理由>
步骤：<按键序列>
记录：<表>
判据：<事先写死，别跑完再定>
结论：<一句话> → 详情写进 WALL-MOUNT-OPEN 的对应节
日志：software/logs/mount_YYYYmmdd_HHMMSS.log
```
