# 地墙过渡：前足触墙与小幅抬身验证

脚本：`software/scripts/ground_wall_probe.py`。配套模型：`software/hexapod/wall_entry.py`。

验证配置逐步开启：`--self-stand`从地面自行站起、一次验证一只前足；再加`--dual-front`验证双前足同时吸墙、四足留地的静态保持与逐足返回；在此基础上加`--pitch-probe`验证0～2°小幅抬头。单独使用`--live`则使用原承托模式，要求六足确认吸附后才能抬身。都不提供中后足转墙或整机自由翻身。默认只做离线检查；只有`--live`会打开树莓派硬件。离线PASS只代表当前模型和控制流程通过，不代表实物无碰撞或可承重。

## prepare剐蹭修正：先在远处抬高，再向墙接近

用户反馈：L1吸墙后，R1在`prepare R1`过程中剐蹭墙面，并观察到抬前足时机身前部下沉。旧轨迹先升到离地50 mm，再斜向上接近墙前20 mm，升高和靠墙同时发生。尚无实测姿态数据，不能仅凭此现象把原因全部归为下沉。

新轨迹对L1/R1一致生效：先保持原地面落脚点的X/Y，竖直抬到墙面目标高度（默认224 mm）；再保持该高度移动到墙前准备位。新增`--approach-gap`设置准备位吸盘中心的模型墙距，默认由20 mm增为60 mm，可配置20～80 mm；整段不满足IK、原始脉宽余量或关节中心净空时，预检会拒绝，不能靠扩大参数范围绕过。

默认几何下60 mm的准备、触墙及返回序列已通过离线检查；增至80 mm时L1准备轨迹触发舵机脉宽余量限制，会被拒绝。因此先使用60 mm，不应认为参数允许填写的所有距离都存在可执行路径。

默认自行站立几何下，抬高段吸盘中心的模型墙距约85.4 mm，随后在目标高度接近至60 mm；原轨迹最后仅20 mm。新准备位的模型膝关节X约80.0 mm，相比旧准备位112.3 mm后移约32.3 mm（X越大越靠墙）。三自由度腿的足端位置和膝位互相约束，不能保持足端完全不动而独立把膝盖向后挪。新准备位吸盘轴与墙法向的模型夹角约8°，`touch`到墙面时约0.26°；准备位不必已完全对正。

**`touch`现在默认从60 mm准备位推进到模型墙面，行程随`--approach-gap`变化。** 返回时先在目标高度退到准备位，再移到原落脚点正上方，最后竖直落地；不会贴近墙面向下撤腿。60 mm是虚拟吸盘中心间距，不是测得的唇边、气管或打印件净空。模型没有实时机身高度/俯仰反馈，这次修改增加运动余量，不会纠正机身下沉。

先结束旧进程、回到未吸附趴姿，并同步脚本与`hexapod/wall_entry.py`，使用下述配置离线检查，再实机重测双前足准备动作：

```bash
python3 software/scripts/ground_wall_probe.py --self-stand --dual-front --approach-gap 60 --mock --demo --report /tmp/wall-entry-clearance.json
python3 software/scripts/ground_wall_probe.py --live --self-stand --dual-front --approach-gap 60
```

先重测L1完整触墙、吸附、释放返回，再建立L1吸附并观察`prepare R1`全过程。`SEGMENT complete`后先停留，记录吸盘实际墙距、前后髋高度和侧面视频，确认无剐蹭、机身没有继续下沉后才执行`touch R1`。`status`新增`approach_gap_mm`和`commanded_front_wall_gap_mm`，二者都是模型数据，不能代替尺量。若前部仍明显下沉或继续剐蹭，`stop`并由承托接管，记录当时姿态后再调整；本轮先复核准备与返回路径，再继续抬头测试。

## 自行站立模式（无需机身下方垫支撑物）

2026-09-07新增。启动的蹲姿与站姿采用`stand_up.py`相同的地面落足半径130 mm；六足在地面上的指令落点固定，机身髋高从20 mm缓慢升到默认90 mm。默认约11秒。它不是把六只脚在空中收起再踩下；第一拍使能仍与`stand_up.py`一样，发预设蹲姿，不能读取真实关节位置。

1. 将机器人以原来运行`stand_up.py`前的自然趴姿放在平地上，头朝光滑墙面，机身与墙面摆正，前髋轴距墙160 mm。停止其他控制程序、确保没有脚吸住墙面。无需在机身下垫块，首次前足测试保留防跌安全绳，正常时不承重。
2. 先离线运行：

```bash
python3 software/scripts/ground_wall_probe.py --self-stand --mock --demo --report /tmp/wall-entry-self-stand.json
```

3. 实机运行：

```bash
python3 software/scripts/ground_wall_probe.py --live --self-stand
```

4. 输入`start`并回车，等待`SEGMENT complete`，让六足把机身撑起来。
5. 量实际髋轴离地高度与前髋到墙距离。**90 mm是开环运动学目标，没有高度传感器或姿态反馈，吸盘压缩、齿隙和带载挠曲会造成偏差。** 如果实测明显不符，先记录实际值并校正模型/站姿，不要把日志中的90 mm当成实际测量值继续触墙。
6. 一次只测一只前足：`prepare L1` → `touch L1` → 分次`press L1 2` → `attach L1` → `release L1` → `return L1`。每段完成后再发下一条；其他五足保持地面支撑指令。确认L1回地面后才能测试R1。
7. 两只前足都返回且已放气后，输入`sit`，等机身缓慢降到目标20 mm蹲姿；随后`quit`断电退出。

限制：单独使用`--self-stand`禁止同时把两前足移出地面、禁止地面足压紧/抽气、禁止`pitch`。它依赖现有机器人站立与五足支撑能力，不做吸附足接管重量的验证；五足留在地面是指令约束，没有传感器确认真实载荷或接触。双前足静态实验及小幅抬头分别见后续章节，需显式增加对应参数。

`stop`和异常冻结不会自动退腿或趴下；先扶稳机身再取机。`quit`/Ctrl-C/EOF仍会断舵机电，若未完成`sit`，机身会失去主动支撑。不能在已经站立/挂墙时重启脚本接管，因为初始化会断电并排气。

## 下一阶段：双前足吸墙、四足留地静态保持

2026-09-07用户反馈：L1单足完整触墙、吸附、释放、返回流程正常；R1随后确认吸附。两次均为指令髋高90 mm、压入2 mm；L1压力−66.681 kPa，R1压力−73.656 kPa，采样时泵关闭、电流均0.8954 A。

随后双前足实测出现`HOLD confirmed L1 R1`，两足连续10秒≤−50 kPa。结束时L1为−63.312 kPa、R1为−69.369 kPa，两足指令压入均2 mm，母线7.889 V、电流1.7908 A、泵关闭、无冻结。用户确认完整返回已完成、表现良好。未提供视频或明确的承托受力记录；这些结果不能换算为额定承重或抗剥离能力。

本阶段目标：先L1吸墙，再R1吸墙，双足连续10秒保持合格压力，随后逐足返回。机身保持水平及90 mm指令高度，四只中后足指令落点不变；禁止`pitch`、中后足移动与地面抽气。第二只前足离地后，中后四足的模型支撑区域前缘约在机身原点X=0处，实际重心与接触载荷未知，不能把四足留地当作静态稳定证明。

**准备能接住整机的安全绳或机身承托，观察是否介入承重。** 若由承托分担重量，记录为承托下测试；不要通过拉扯机身或继续抬脚来检验吸力。保留侧面视频，观察四只地面足是否滑移、机身是否倾斜下沉、墙面吸盘是否翘边及结构挠曲。

先结束旧进程：若R1仍吸墙，扶稳并卸载后`release R1`，等释放确认再`return R1`；六足回地面后`sit`，完成后`quit`。将更新后的`software/hexapod/wall_entry.py`与`software/scripts/ground_wall_probe.py`一起同步到树莓派。新模式不能在旧进程中切换，也不能从吸墙状态重新初始化。

先离线验证，再从未吸附的地面趴姿启动：

```bash
python3 software/scripts/ground_wall_probe.py --self-stand --dual-front --mock --demo --report /tmp/wall-entry-dual-front.json
python3 software/scripts/ground_wall_probe.py --live --self-stand --dual-front
```

双前足配置默认压入上限改为2 mm，沿用本轮单足实测指令；再次输入`press LEG 2`会因累计超限而拒绝。其他模式默认仍为18 mm。`--max-press`可以显式改变上限，但本轮先使用默认2 mm；不能把增加压入量当作吸附失败的自动修复。

下表每一条命令独立输入，等完成再继续，不要整段粘贴：

| 顺序 | 命令 | 完成条件 |
|---|---|---|
| 1 | `start` | `SEGMENT complete`；核对实物髋高和墙距 |
| 2 | `prepare L1` → `touch L1` → `press L1 2` | 每段完成，观察贴合与机身姿态 |
| 3 | `attach L1` | `ATTACH confirmed L1` |
| 4 | `prepare R1` → `touch R1` → `press R1 2` | L1保持吸附，四只中后足留地；每段观察是否滑移、倾斜 |
| 5 | `attach R1` | `ATTACH confirmed R1` |
| 6 | `hold` | `HOLD confirmed L1 R1`，然后`status`记录两足压力和电源数据 |
| 7 | `release R1` | 扶稳、卸载R1后执行，等`RELEASE confirmed R1`；L1保持吸附 |
| 8 | `return R1` | `SEGMENT complete`，目视确认回到地面 |
| 9 | `release L1` → `return L1` | 依次等待释放确认、返回完成 |
| 10 | `sit` → `quit` | 等趴下完成后再退出断电 |

也支持先R1后L1、或先返回L1。软件要求另一只前足已回地面或已确认吸墙，才允许该前足准备、释放或返回；因此不能两足先抬起再抽气，也不能连续释放两只墙足再一起返回。地面接触条件只是指令状态检查，仍需目视确认。

`hold`不发送运动帧；两只墙足必须连续10秒≤−50 kPa。中途任一足回升到−50～−30 kPa会重置连续计时并允许补抽，30秒内无法完成则冻结；超过−30 kPa立即冻结。允许泵运行，因此通过只代表监测期间密封达标，不代表泵关闭保压或承载能力达标。`status`/日志的`waiting`、`hold_good_s`显示保持进度，完成事件写入日志。

第二只脚抬起或返回过程中，另一墙足压力不合格会暂停轨迹；严重漏气、超时、供电异常或`stop`会冻结，保留阀状态、停泵，无自动回位或解除吸附。此时先承托整机，再人工卸载取机；不要把`quit`当作趴下命令。双足静态实验通过且没有地面滑移、姿态变化或承托接管后，再结合视频与日志设计下一步小幅姿态变化和载荷转移实验。

## 再下一阶段：双前足吸墙时小幅抬头并回到水平

在单足循环、双前足静态保持与逐足返回已完成的基础上，新增`--pitch-probe`。本阶段观察固定足端目标时机身小幅转动的表现；四只中后足继续留地承重，不能让两只前足提起整机。仍需能接住整机的安全绳或承托，记录是否分担重量，支撑装置应允许本轮小角度运动而非把机身夹死。

**第一次只做0°→0.5°→0°。** `pitch`是绝对目标角，不是累加值。每次变化最多0.5°，角速度模型峰值不超过0.25°/s；0.5°段默认约3秒。新模式硬上限2°（`--pitch-limit`可降低上限，不能将其扩大到2°以上），每次继续增加角度前必须在当前姿态完成一次`hold`。向0°回退不要求新做10秒保持，但两只墙足仍需压力合格；没有自动退回水平或故障恢复。

六个世界坐标足端指令点保持固定，机身绕身体原点抬头，原点高度保持90 mm。**原日志`commanded_hip_height_mm`在倾斜时表示原点高度，不能理解为所有髋轴仍离地90 mm。** 新增`commanded_body_origin_height_mm`及`commanded_hip_heights_mm`：0.5°时模型前髋约90.729 mm、后髋约89.271 mm；这些仍是指令值，没有IMU或实际关节位置反馈。地面足未抽气，其吸盘姿态和实际接触点会受顺应性影响，软件固定目标不代表实物不会滚动、滑动或翘边。

默认几何、两足压入2 mm时，0～2°及返回路径通过当前IK、未截断脉宽、墙足轴向和关节中心净空采样检查；2°时两墙足模型轴向偏差约1.98°。未验证实体碰撞、接触力及载荷分配。每次`pitch`执行前还会预检该目标返回0°的几何路径；这个检查不自动发送回程指令。实机启动预检覆盖本模式的完整允许角度和逐足返回，不会因为`--demo-pitch 0`而只检查静态段。

同步更新脚本及`hexapod/wall_entry.py`，结束原运行、六足回到未吸附趴姿后，先离线检查再实机运行：

```bash
python3 software/scripts/ground_wall_probe.py --self-stand --dual-front --pitch-probe --mock --demo --report /tmp/wall-entry-pitch-probe.json
python3 software/scripts/ground_wall_probe.py --live --self-stand --dual-front --pitch-probe
```

离线demo检查0→0.5→1→1.5→2→1.5→1→0.5→0及各姿态保持、释放返回和趴下。它不操作硬件；实机初轮按下表只试0.5°，不要照搬整个demo角度序列。

| 阶段 | 逐条输入的命令 | 观察和完成条件 |
|---|---|---|
| 建立双足吸附 | `start`；按上一节依次完成L1、R1的`prepare`/`touch`/`press 2`/`attach` | 每段完成，两足各自确认吸附 |
| 水平基线 | `hold`，完成后`status` | 记录两足压力、电流和实际机身姿态 |
| 首次抬头 | `pitch 0.5` | 等`SEGMENT complete`；观察机身、地面足、吸盘边缘和结构 |
| 小角度保持 | `hold`，完成后`status` | 两足连续10秒压力达标，并记录物理表现 |
| 回到水平 | `pitch 0` | 等`SEGMENT complete`，目视核对姿态 |
| 水平复查 | `hold`，完成后`status` | 对比开始与回程后的读数和外观 |
| 收尾 | `release R1`→`return R1`→`release L1`→`return L1`→`sit`→`quit` | 每条独立输入；先等释放确认再收腿，先等回地面再释放另一足，趴下后退出 |

`hold_confirmed_pitch_deg`显示当前仍有效的保持角度；动作、放气或采样到墙足压力回升到−50 kPa以上会使结果失效，不能用旧结果继续加大角度。抬头时禁止释放、回腿及`sit`，先以每步≤0.5°回到0°。若已冻结，无法再执行这些回程命令，必须先由承托接管整机后人工取机。

第一次0.5°往返完成后，记录压力、电流与视频，再决定是否递增至1°、1.5°和2°；每次增加前重新`hold`。出现明显机身下沉、地面足滑移、墙足翘边、结构干涉时`stop`并承托；即使压力未下降也不能判为通过。该阶段完成后才继续设计机身平移或中足转墙路径。


## 1. 承托模式的台架与坐标

- 墙面在机头正前方，竖直玻璃或光滑板；地面若要做六足吸附/抬身实验，也要能吸附。无踢脚线、无中间缝隙障碍。
- **整个实验都要有独立机身承托，并沿用安全绳。** 承托须能承受整机重量；抬身测试时允许机身小角度运动，不能把机身刚性夹死后让舵机硬顶。这里尚未求解载荷分配，不可撤去承托来验证自行翻身。
- 默认机身髋轴平面离地 **90 mm**；机身水平时，左右前髋轴到墙 **160 mm**，不是机壳前缘到墙。墙上目标唇口中心离地 **224 mm**；两足目标横向位置分别与各自前髋对齐。
- `start` 的初始停放姿态使用支撑半径约177 mm、足端在髋轴下20 mm；随后慢速伸到地面。上电前承托机身，让腿接近此停放姿态并留出空间：没有舵机位置回读，**首次使能不是从测得的当前姿态插值**。
- **只能从未吸附、卸载的冷启动状态运行。** 驱动初始化会断舵机电；气路初始化会让六足排气。不能在机器人已经挂墙时启动此脚本，也不能与 `climb_walk`、`body_lean` 或语音控制同时运行。

坐标：世界+X朝墙、+Z朝上。`pitch 1` 是机头向上1°，绕机身坐标原点、保持原点高度；不是绕后髋旋转，也不包含身体向墙平移。日志里的足端是自由吸盘模型的**虚拟指令点**，包含压入超程，不是测量出的实际唇口位置。

## 2. 先离线检查

在仓库根目录运行；`python3` 应使用项目已安装依赖的解释器（开发机也可用 `.venv/bin/python`）：

```bash
python3 software/scripts/ground_wall_probe.py --plan --scenario front --report /tmp/wall-entry-front.json
python3 software/scripts/ground_wall_probe.py --mock --demo --scenario mixed --report /tmp/wall-entry-mixed.json
```

第一条检查L1完整触墙、压紧、吸附、释放、回地面的序列。第二条使用理想密封模型，检查两前足上墙、四足地面吸附、抬头1°/2°、回到0°、前足返回。mock里的成功吸附不依赖真实压紧深度，不能用来证明2 mm或18 mm足够。

JSON记录每段指令、六足虚拟目标、压力、泵阀状态、压入量和机身俯仰；拒绝时返回非零退出码及失败指令。调整台架几何后先重跑，例如：

```bash
python3 software/scripts/ground_wall_probe.py --plan --distance 160 --height 220 --report /tmp/wall-entry-220.json
```

所有轨迹使用当前逐舵机标定；检查整段采样轨迹的IK、未截断脉宽（保留50 µs电气余量）、接触时吸盘轴角≤15°、关节中心到地/墙的净空。未检查实体网格、气管、带载变形与真实机械限位。15°是模型门槛，不是本轮重新测得的容差。

## 3. 承托模式的前足触墙：先L1，再R1

树莓派、承托到位后，在终端运行：

```bash
python3 software/scripts/ground_wall_probe.py --live --distance 160 --height 224
```

可用 `--mock` 代替 `--live` 熟悉交互。每条命令按回车；等待 `SEGMENT complete` 或吸附确认后再输入下一条，**不要整段粘贴**。`status` 随时查看压力/状态，`help` 查看帮助。

| 命令 | 动作及观察点 |
|---|---|
| `start` | 发停放姿态、使能舵机、缓慢伸腿至地面；确认承托位置和实体间隙 |
| `prepare L1` | L1先在原落脚点上方抬至目标高度，再在该高度移到墙前60 mm（`--approach-gap`）；观察大腿、小腿、气管干涉 |
| `touch L1` | 从准备位推进到模型墙面，默认行程60 mm；先检查实际间距，观察唇口接触与贴合 |
| `press L1 2` | 沿墙法向增加2 mm虚拟压入；必要时逐次重复，默认累计最多18 mm |
| `attach L1` | 抽气；压力连续0.5 s≤−50 kPa才确认。必须已压入至少2 mm；具体需要多少由实测决定 |
| `status` | 查看L1压力、当前压入量、电压、电流、泵阀状态 |
| `release L1` | 承托/卸载后放气；连续0.5 s≥−5 kPa并完成排气状态机才确认 |
| `return L1` | 确认放气后沿法向撤回墙前，再经空中路径返回地面 |

L1通过后，将上述腿名换成R1单独验证。`attach`失败会冻结，没有自动加深、重试或撤腿。冻住后承托取机、退出、调整后重开；不要在仍吸附悬挂的状态重启程序。

压紧动作只在吸附前允许；吸附后不会继续盲目加压。单次压入增量可取0～2 mm之间的正数，总量不超过`--max-press`（允许2～18 mm）。测到实体干涉、持续电流增长、唇边翻折或机身被拉动就停止当前实验，不把继续增加压入当作修复。

## 4. 承托模式的六足混合接触与小幅抬身

前提：前足单独触墙/释放已验证，地面可吸附，承托能跟随机身微小转动。新开一轮：

1. `start`。
2. L1执行 `prepare` → `touch` → 分次`press` → `attach`，保持吸附。
3. R1执行同样的序列，保持吸附。
4. 对L2、L3、R2、R3分别执行分次`press LEG 2` → `attach LEG`。地面足的压紧方向是向下；不要对它们使用`prepare`。一次只处理一只脚，前一只确认后再处理下一只。
5. `status`确认六足吸附。输入`pitch 1`，观察并停留；正常后`pitch 2`。回程依次`pitch 1`、`pitch 0`。

`pitch`是绝对角度，每次变化最多1°，默认范围0～5°（可配置上限最多10°；这只是实验限幅）。整个路径先检查再执行；拒绝则当前姿态不动、不会截断到限位。六足目标点在世界系固定，脚本重新计算各腿角度。固定的是虚拟指令锚点，**不代表实物足端完全不动或载荷已平衡**。

只有两只前足在墙上、四足在地面且六足均通过压力确认才允许`pitch`。回到0°后才允许单足操作。此轮不提供中/后足转墙指令。

## 5. 停止、取机和日志

- `stop`：清空轨迹、停止泵，保留当前阀输出；舵机暂时保持最后指令。进入冻结后不自动恢复，输入`quit`收尾。停泵不能保证长期保压，必须有承托。
- 已吸附足压力回升到−50～−30 kPa：暂停推进，允许补抽；10 s未恢复则冻结。超过−30 kPa：立即冻结。
- 吸附/排气等待最长10 s，吸附状态机本身的抽气超时为2.5 s。传感器异常/读数非法会冻结；不再发送下一帧腿部指令。
- `start`上电完成后，舵机母线低于配置截止线6.0 V，或电流高于配置告警线8 A持续0.3 s：冻结并切舵机电。这里读的是Servo2040母线遥测，不是每个关节的力矩。上电后的检查在发送下一帧运动指令之前执行。
- `release LEG`用于选定足放气，先承托/卸载；有倾角时先正常回到0°。若故障已经冻结，不再允许自动释放或回位，先物理承托取机。
- `quit`、Ctrl-C或终端EOF：停泵、六阀线圈断电、舵机断电。**不自动放气、不自动回位，也不能据此保证仍吸附或保持原姿态。** 承托模式退出前必须独立承托机身；自行站立模式正常结束应先完成`sit`，未趴下或已冻结则先扶稳取机。阀断电为接通真空位置；需要解除吸附时在承托后手动排气。
- 重新运行时气路初始化会排气，所以不能把“重启脚本”当作挂墙续跑。

黑匣子写入 `software/logs/wall_entry_日期_时间.log`，每秒记录六足压力、虚拟目标、俯仰、压入量、泵阀、电压/电流；事件实时落盘。树莓派另启用项目现有PowerWatch电源监测。用`--log-dir`更改目录。

如果旧版在输入`start`之前就报`Battery below cutoff`，是脚本在舵机继电器尚未合闸时也执行了欠压判断；此时母线低电压不能代表电池端欠压。输入`quit`退出，更新脚本及`hexapod/wall_entry.py`后重新启动。修正版在等待`start`时仍读取遥测，但只在上电后启用欠压/过流判断；`status`和日志中的`started`表示启动流程是否完成，并非继电器反馈。若上电后仍报欠压，按报错中的实测母线电压检查电池及供电线路，不能通过调低截止线绕过。

建议拍摄连续侧面视频，画面包括墙角、前足、机身及承托。记录每次实际髋高/墙距、压入量、是否由承托分担重量，避免把“承托下完成”误记成独立过渡成功。后续最有价值的数据是：两前足压紧成功的姿态范围、各阶段电流与掉压、抬头时哪一只脚先倾斜/剥离。
